enum PlatformType {
  SLACK
}

type TeamProvider {
  type: PlatformType
  access_token: String
  provider_id: String
  name: String
}

type Team {
  team_id: String!
  name: String!
  providers: [TeamProvider]
}
