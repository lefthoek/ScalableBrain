enum ProviderType {
  SLACK
}

type TeamProvider {
  type: ProviderType!
  id: String!
  name: String!
  access_token: String!
}

type Team {
  id: String!
  name: String!
  providers: [TeamProvider!]
}
