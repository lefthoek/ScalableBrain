schema { 
  query: Query  
  subscription: Subscription  
}

scalar EmailAddress

scalar GUID

scalar JSON

type Query {
  team(teamId: String): Team
}

type Subscription {
  addedTeams: Team!
  updatedTeam(id: String!): Team!
}

type Team {
  id: String!
  name: String!
}

scalar URL
