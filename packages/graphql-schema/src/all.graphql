schema { 
  query: Query  
  subscription: Subscription  
}

enum PlatformType {
  SLACK
}

type Query {
  team(teamId: String): Team
}

type Subscription {
  addedTeams: Team!
  updatedTeam(id: String!): Team!
}

type Team {
  id: String!
  name: String!
  providers: [TeamProvider]
}

type TeamProvider {
  type: PlatformType
  access_token: String
  id: String
  name: String
}
