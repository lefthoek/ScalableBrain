schema { 
  query: Query  
  subscription: Subscription  
}

type Query {
  team(teamId: String): Team
}

type Subscription {
  addedTeams: Team!
  updatedTeam(id: String!): Team!
}

type Team {
  id: String!
  name: String!
}
