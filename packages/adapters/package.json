{
  "name": "@lefthoek/adapters",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "author": "Jan Hein Hoogstad",
  "license": "MIT",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "test": "jest --verbose --silent",
    "watch": "tsc -w"
  },
  "dependencies": {
    "aws-sdk": "^2.1058.0"
  },
  "devDependencies": {
    "@lefthoek/types": "*",
    "@lefthoek/config": "*",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
